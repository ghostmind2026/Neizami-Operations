name: neizami_operations
description: Neizami Operations Android client for Neizami Mobile Bridge.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.5.0
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.5+1
  local_auth: ^2.3.0
  cached_network_image: ^3.4.1
  image_picker: ^1.2.0
  webview_flutter: ^4.13.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
